import 'package:flutter/material.dart';

class TaskpostPage extends StatelessWidget {
  const TaskpostPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(child: Text("Task Post Page"));
  }
}